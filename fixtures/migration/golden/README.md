# Golden Output Directory

This directory will be populated by the golden-recording task. Subdirectories exist for each output category:

- `analytics/` — Golden outputs from analytics endpoints
- `qmd/` — Golden QMD progress event payloads (Scenario 6 samples included)
- `qmd-logs/` — Golden QMD log extraction outputs
- `provider-limits/` — Golden provider limits outputs
